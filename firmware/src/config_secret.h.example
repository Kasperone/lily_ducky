#pragma once
// Copy this file to config_secret.h (gitignored) and set real values.
// config_secret.h is included by config.h and must define both macros.
#define CFG_WIFI_SSID "LilyC2"
#define CFG_WIFI_PASS "CHANGE_ME"
